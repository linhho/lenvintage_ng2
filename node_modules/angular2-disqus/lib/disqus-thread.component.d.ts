import { OnChanges, SimpleChanges } from '@angular/core';
export declare class DisqusThreadComponent implements OnChanges {
    pageIdentifier: string;
    pageUrl: string;
    constructor();
    ngOnChanges(changes: SimpleChanges): void;
}
